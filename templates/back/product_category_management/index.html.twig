{% extends 'back/base.html.twig' %} 

{% block javascripts %}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>

{% endblock %}

{% block title %}{% endblock %}

 


{% block body %}


 <section class="mt-[14rem] ml-[18rem] flex justify-center z-[100] fixed sm:w-full sm:ml-[0] sm:mt-[14rem]">           
            
        <div id="newCategory" class="container relative z-10 flex flex-col items-center hidden">

            <div class="w-[42.5rem] rounded-[1.5rem] shadow-lg mt-[-12.5rem] relative z-20 bg-white">

                <div class="w-[42.5rem]   p-5 waterGradient rounded-t-[1.5rem] mb-[0.9rem]">
                    <h4 class="mb-0 text-center font-medium text-2xl text-white">Nouvelle Catégorie</h4>
                </div>


                {{ form_start(formCategory) }}
                <div class="flex flex-row  justify-around items-center">
                    <div>
                        {{ form_row(formCategory.name, {
                            'label_attr':{'class' : 'text-base  ml-10'},
                            'attr': {

                                'class': 'w-[290px] mt-3 mb-[10px] ml-10 p-2 rounded-[25px] border border-solid border-[#A0A0A0] outline-none',
                                'placeholder': 'Nom de la catégorie'
                            }
                        }) }}


                        
                    </div>

                    <div>
                        {{ form_row(formCategory.description,{
                            'label_attr':{'class' : 'text-base  ml-10'},
                            'attr': {
                                'class': 'w-[290px] mt-3 mb-[10px] ml-10 p-2 rounded-[25px] border border-solid border-[#A0A0A0] outline-none',
                                'placeholder': 'Description'
                            }
                        }) }}
                    </div>

                  

                    
                    
                </div>


                <div class="flex flex-row justify-center">
                    {# <button type="submit" class="btn-primary">Enregistrer</button> #}
                
                    
                        {{ form_row(formCategory.Enregistrer,{
                            'attr': {
                                'class': 'btn-primary mt-4 mb-8',
                            }
                               }) 
                        }}
                    
                    <span  class="btn-primary mt-4 mb-8" onclick="cacherFormulaire('newCategory')">Annuler</span> 
                   
                </div>

                {{ form_end(formCategory) }}

            </div>

        </div>
</section>


    <section class="mt-[14rem] ml-[18rem] flex justify-center z-[100] fixed sm:w-full sm:ml-[0] sm:mt-[14rem]">

        <div id="updateCategory" class="container relative z-10 flex flex-col items-center hidden">

            <div class="w-[42.5rem] rounded-[1.5rem] shadow-lg mt-[-12.5rem] relative z-20 bg-white">

                <div class="w-[42.5rem]   p-5 waterGradient rounded-t-[1.5rem] mb-[0.9rem]">
                    <h4 class="mb-0 text-center font-medium text-2xl text-white">Modofier La Catégorie</h4>
                </div>


                {{ form_start(formCategoryUpdate) }}
                <div class="flex flex-row  justify-around items-center">
                    <div>
                        {{ form_row(formCategoryUpdate.name, {
                            'label_attr':{'class' : 'text-base  ml-10'},
                            'attr': {

                                'class': 'w-[290px] mt-3 mb-[10px] ml-10 p-2 rounded-[25px] border border-solid border-[#A0A0A0] outline-none',
                                'placeholder': 'Nom de la catégorie'
                            }
                        }) }}
                    </div>

                    <div>
                        {{ form_row(formCategoryUpdate.description,{
                            'label_attr':{'class' : 'text-base  ml-10'},
                            'attr': {
                                'class': 'w-[290px] mt-3 mb-[10px] ml-10 p-2 rounded-[25px] border border-solid border-[#A0A0A0] outline-none',
                                'placeholder': 'Description'
                            }
                        }) }}
                    </div>






                </div>
                {{ form_row(formCategoryUpdate.id) }}

                <div class="flex flex-row justify-center">
                    {# <button type="submit" class="btn-primary">Enregistrer</button> #}


                    {{ form_row(formCategoryUpdate.Enregistrer,{
                        'attr': {
                            'class': 'btn-primary mt-4 mb-8',
                        }
                    })
                    }}

                    <span  class="btn-primary mt-4 mb-8" onclick="cacherFormulaire('updateCategory')">Annuler</span>

                </div>

                {{ form_end(formCategoryUpdate) }}

            </div>

        </div>
    </section>




<div class="flex mt-4 ml-4 justify-between ">
    <div class="flex justify-start sm:w-1/2 ">
        <svg class="mt-2" width="14" height="24" viewBox="0 0 14 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M13.4125 10.5875C14.1938 11.3687 14.1938 12.6375 13.4125 13.4187L3.41255 23.4187C2.6313 24.2 1.36255 24.2 0.581299 23.4187C-0.199951 22.6375 -0.199951 21.3687 0.581299 20.5875L9.1688 12L0.587549 3.41249C-0.193701 2.63124 -0.193701 1.36249 0.587549 0.581238C1.3688 -0.200012 2.63755 -0.200012 3.4188 0.581238L13.4188 10.5812L13.4125 10.5875Z" fill="#D9DDDF"/>
        </svg>  

        <h2 class="text-gray-300 ">Catégories</h2>
    </div>
</div>
    


<div class="drop-shadow-lg mx-5 mt-6 bg-white p-10 ">
   <div class=" flex flex-row justify-between items-center">
        {% include"back/blocks/search_data.html.twig" with{
            form:formSearchCategory
        } %}

        <button class="btn-primary " onclick="afficherFormulaire('newCategory')"> Nouvelle Catégorie </button>

   </div>
        <div class="overflow-x-auto mt-8">
            <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
                <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                        <tr>
                            <th scope="col" class="px-6 py-3 text-center">Numéro</th>
                            <th scope="col" class="px-6 py-3 text-center">Catégorie</th>
                            <th scope="col" class="px-6 py-3 text-center">Description</th>
                            <th scope="col" class="px-6 py-3 text-center">Nombre des produits</th>
                            <th scope="col" class="px-6 py-3 text-center">Date création</th>
                            <th scope="col" class="px-6 py-3 text-center">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for category in categories %}
                        <tr class="odd:bg-white odd:dark:bg-gray-900 even:bg-gray-50 even:dark:bg-gray-800 border-b dark:border-gray-700">

                            <td class="px-6 py-4 text-center">{{ category[0].id }}</td>
                            <td class="px-6 py-4 text-center">{{ category[0].name }}</td>
                            <td class="px-6 py-4 text-center">{{category[0].description}}</td>
                            <td class="px-6 py-4 text-center">{{category.productCount}}</td>
                            <td class="px-6 py-4 text-center">{{ category[0].createdAt|date('Y-m-d H:i:s')}}</td>
                            <td class="px-6 py-4 flex justify-around items-center">
                            <span onclick="deleteCat({{ category[0].id }})" ><img src="https://i.goopics.net/08xic0.png" alt="Icon Supprimer" class="w-6 cursor-pointer"></span>
                            <span onclick="updateCat('updateCategory','{{ category[0].id }}','{{ category[0].name }}','{{category[0].description}}')"><img src="https://i.goopics.net/s6fgvf.png" alt="Icon Modifier" class="w-6 ml-2 cursor-pointer"></span>

                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Pagination -->
        <div class="flex justify-center items-center mt-4">
            <nav class="block">
                {{ knp_pagination_render(categories) }}
            </nav>
        </div>
</div>


<style>
    /* Ajoutez cette section pour définir l'animation fadeInDown */
    @keyframes fadeInDown {
        from {
            
            opacity: 0;
            -webkit-transform: translatey(-100px);
            -moz-transform: translatey(-100px);
            -o-transform: translatey(-100px);
            transform: translatey(-100px);
        }
        to {
            
            opacity: 1;
            -webkit-transform: translatey(0px);
            -moz-transform: translatey(0px);
            -o-transform: translatey(0px);
            transform: translatey(0px);
        }
    }

    @keyframes fadeOutUp {
        from {
            opacity: 1;
        }
        to {
            opacity: 0;
            transform: translateY(-50px);
        }
    }

    .fadeOutUp {
        animation: fadeOutUp 1s;
    }
    
</style>

<script>

   

    // Fonction pour afficher le formulaire avec une animation de haut en bas
    function afficherFormulaire(formulaireId) {
        var formulaire = document.getElementById(formulaireId);
        formulaire.classList.remove('hidden');
        formulaire.style.animation = 'fadeInDown 0.5s';

        // Ajoutez une classe d'animation pour faire descendre le formulaire
        formulaire.classList.add('fadeInDown');

        // Supprimez la classe d'animation après l'achèvement pour la prochaine utilisation
        setTimeout(function() {
            formulaire.classList.remove('fadeInDown');
        }, 500);
    }

    function cacherFormulaire(formulaireId) {
        var formulaire = document.getElementById(formulaireId);
        formulaire.classList.add('fadeOutUp');

        setTimeout(function() {
            formulaire.classList.add('hidden');
            formulaire.classList.remove('fadeOutUp');
        }, 500);
    }

    // AJAX for deleting a category
    function updateCat(formId,id,name,desc) {

        $("#edit_category_name").val(name);
        $("#edit_category_description").val(desc);
        $("#edit_category_id").val(id);
        afficherFormulaire(formId);
    }

    function deleteCat(id) {
        var categoryId = $(this).data('id');
        console.log(id);
        $.ajax({
            type: 'DELETE',
            url: 'gestion-prod-cat/delete-cat/'+id,
            success: function (response) {
                if (response.success) {
                    console.log(categoryId);
                    // Reload the page or update the category list
                    window.location.reload();
                    console.log('success');
                } else {
                    // Handle errors
                    console.error('Failed to delete category.');
                }
            }
        });
    };
    


</script>



{% endblock %}
