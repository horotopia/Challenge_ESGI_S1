<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapport ventes par Client</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            position: relative;
            margin: 0;
        }

        @page {
            margin: 0;
            size: auto;
        }

        .container {
            width: 100%;
            padding: 10px;
            box-sizing: border-box;
        }

        .header {
            text-align: center;
        }

        .header h1 {
            background-color: #3498db;
            color: #fff;
            margin: 0;
            padding: 20px 0;
        }

        .info .company p {
            margin: 5px 0;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            border: 1px solid rgb(230, 228, 228);
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #3498db;
            color: #fff;
        }

        .footer {
            text-align: center;
            margin-top: 20px;
        }

        .footer p {
            margin: 5px 0;
            color: #cecece;
            font-size: 12px;
        }
    </style>
</head>

<body>
<div class="container">
    <div class="header">
        <h1>Rapport ventes par Client</h1>
        <div class="info">
            <div class="company">
                <p><strong>Nom Entreprise:</strong> {{ companyInfo.name }}</p>
                <p><strong>Adresse:</strong> {{ companyInfo.address }}</p>
                <p><strong>Téléphone:</strong> {{ companyInfo.phone }}</p>
            </div>
        </div>

        <table>
            <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Nombre de ventes</th>
                <th>Total TTC des ventes</th>
            </tr>
            </thead>
            <tbody>
            {% for sale in data %}
                <tr>
                    <td>{{ sale.lastName }}</td>
                    <td>{{ sale.firstName }}</td>
                    <td>{{ sale.salesCount }}</td>
                    <td>{{ sale.totalTTC |number_format(2, '.', ',')}}€</td>

                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="footer">
        <hr>
        <p>Conditions générales de vente | Politique de confidentialité</p>
        <p>&copy; 2024 {{ companyInfo.name }}. Tous droits réservés.</p>
    </div>
</div>
</body>

</html>
